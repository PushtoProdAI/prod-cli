# Keep secrets and build outputs out of the Docker build context and image layers.
# Dockerfile.build does `COPY . .`; without this, a local .env (Supabase/Sentry
# values) would land in an intermediate image layer.
.env
.env.*
bin/
dist/
dist-prebuilt/
.git/
