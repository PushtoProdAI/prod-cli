{
  "name": "database-url-constructor",
  "version": "1.0.0",
  "description": "CloudFormation custom resource to construct DATABASE_URL from RDS endpoint and password",
  "main": "index.js",
  "scripts": {
    "build": "npm install --production && zip -r function.zip . -x '*.git*' 'node_modules/.bin/*'",
    "test": "node test.js"
  },
  "dependencies": {
    "@aws-sdk/client-secrets-manager": "^3.400.0",
    "@aws-sdk/client-rds": "^3.400.0"
  },
  "devDependencies": {},
  "keywords": ["cloudformation", "custom-resource", "lambda"],
  "author": "Prod",
  "license": "MIT"
}
